# sequelize_created_read

## Build Setup

```bash
# install dependencies
$ npm install

# create Contact command

node . --create:Contact --firstname=yourname --lastname=yourlastname --phone=yourphone --email='youremail'

# read Contact command

node . --read:Contact

```
